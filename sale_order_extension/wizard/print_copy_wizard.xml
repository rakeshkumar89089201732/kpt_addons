<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_print_copy_wizard_form" model="ir.ui.view">
        <field name="name">print.copy.wizard.form</field>
        <field name="model">print.copy.wizard</field>
        <field name="arch" type="xml">
            <form string="Print Copy Selection">
                <group>
                    <field name="num_copies" widget="radio"/>
                    <field name="report_action_id" invisible="1"/>
                    <field name="record_ids" invisible="1"/>
                    <field name="model_name" invisible="1"/>
                </group>
                <footer>
                    <button string="Print" name="action_print_report" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_print_copy_wizard" model="ir.actions.act_window">
        <field name="name">Select Number of Copies</field>
        <field name="res_model">print.copy.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
