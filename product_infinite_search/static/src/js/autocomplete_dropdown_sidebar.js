/** @odoo-module **/

import { AutoComplete } from "@web/core/autocomplete/autocomplete";

// Use default Odoo positioning (bottom-start with auto-flip) to prevent off-screen issues
// Removed forced "right-start" positioning that was causing dropdown to go off-screen
